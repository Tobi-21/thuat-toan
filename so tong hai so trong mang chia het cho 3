#include <bits/stdc++.h>

using namespace std;
long long n,x,a[100005];
int main()
{
    cin>>n;
    for(int i=1;i<=n;i++)
    {
        cin>>x;
        a[x%3]++;
    }
    cout <<((a[0]-1)*a[0])/2 + a[1]*a[2];
    return 0;
}
